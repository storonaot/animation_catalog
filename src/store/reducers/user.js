const defaultState = {
  firstName: '1233',
  lastName: '456',
  email: '789'
}

export default function user(state = defaultState) {
  // switch (action.type) {
  //   case 'INCREMENT': {
  //     const val = state.val + 1
  //     return { ...state, val }
  //   }
  //   default:
  //     return state
  // }
  return state
}
